# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: src/app/workspace/task-filters.test.ts >> exams filters actual completed-only data on mobile
- Location: tests/e2e/src/app/workspace/task-filters.test.ts:60:7

# Error details

```
Error: expect(locator).toHaveCount(expected) failed

Locator:  getByText('e2e-filter-8aa0fcde5-past-room').filter({ visible: true })
Expected: 1
Received: 3
Timeout:  5000ms

Call log:
  - Expect "toHaveCount" with timeout 5000ms
  - waiting for getByText('e2e-filter-8aa0fcde5-past-room').filter({ visible: true })
    14 × locator resolved to 3 elements
       - unexpected value "3"

```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - generic [ref=e2]:
    - generic [ref=e3]:
      - link "Skip to main content" [ref=e4] [cursor=pointer]:
        - /url: "#main-content"
      - main "Exams" [ref=e6]:
        - generic [ref=e8]:
          - button "Menu" [ref=e9]:
            - generic [ref=e10]: Toggle Sidebar
          - link "Life@USTC" [ref=e11] [cursor=pointer]:
            - /url: /
          - generic [ref=e14]:
            - button "Open search" [ref=e16]
            - generic [ref=e17]:
              - button "Language selector" [ref=e18]
              - button "Theme selector" [ref=e19]
        - region "Main content scroll region" [ref=e20]:
          - generic [ref=e23]:
            - heading "Exams" [level=1] [ref=e27]
            - generic [ref=e28]:
              - group "Exams" [ref=e31]:
                - radio "Upcoming" [ref=e32]
                - radio "Ended" [ref=e33]
                - radio "All" [checked] [ref=e34]
              - generic [ref=e38]:
                - generic [ref=e39]:
                  - link "E2E Filter Course 8aa0fcde5" [ref=e41] [cursor=pointer]:
                    - /url: /catalog/sections/1500000001
                  - paragraph [ref=e43]:
                    - generic [ref=e44]: E2EF8aa0fcde5.01· 2026年春季学期
                    - generic [ref=e45]: "Date: 2026-09-08"
                    - generic [ref=e46]: "Time: 09:00-11:00"
                    - generic [ref=e47]:
                      - text: "Room:"
                      - button "View the location map for E2E-FILTER-8AA0FCDE5-PAST-ROOM" [expanded] [ref=e50]:
                        - generic [ref=e51]: E2E-FILTER-8AA0FCDE5-PAST-ROOM
                    - generic [ref=e52]: Ended
                    - generic [ref=e53]: E2E closed book
                    - generic [ref=e54]: Midterm
                    - generic [ref=e55]: "People: 1"
                - link "More Details" [ref=e57] [cursor=pointer]:
                  - /url: /catalog/sections/1500000001
      - navigation "Mobile primary navigation" [ref=e58]:
        - link "Today" [ref=e59] [cursor=pointer]:
          - /url: /workspace/overview
        - link "Calendar" [ref=e64] [cursor=pointer]:
          - /url: /workspace/calendar
        - link "Tasks" [ref=e68] [cursor=pointer]:
          - /url: /workspace/homeworks
        - link "Explore" [ref=e74] [cursor=pointer]:
          - /url: /catalog/courses
    - region "Notifications alt+T"
  - generic [ref=e81]:
    - generic [ref=e82]: Location map for E2E-FILTER-8AA0FCDE5-PAST-ROOM
    - generic [ref=e83]:
      - generic [ref=e84]:
        - paragraph [ref=e86]: E2E-FILTER-8AA0FCDE5-PAST-ROOM
        - generic [ref=e87]: No map
      - alert [ref=e88]:
        - generic [ref=e89]: A map is not available for this room yet.
      - generic [ref=e91]:
        - button "Open map" [disabled]
```

# Test source

```ts
  1   | import { expect, type Locator, test } from "@playwright/test";
  2   | import { gotoAndWaitForReady } from "../../../utils/page-ready";
  3   | import { createWorkspaceTaskFilterFixture } from "../../../utils/workspace-task-filters";
  4   | 
  5   | async function expectSelection(group: Locator, selected: string) {
  6   |   const states = await group.getByRole("radio").evaluateAll((elements) =>
  7   |     elements.map((element) => ({
  8   |       value: element.getAttribute("data-value"),
  9   |       checked: element.getAttribute("aria-checked"),
  10  |       state: element.getAttribute("data-state"),
  11  |       background: getComputedStyle(element).backgroundColor,
  12  |     })),
  13  |   );
  14  |   for (const active of [
  15  |     states.filter((state) => state.checked === "true"),
  16  |     states.filter((state) => state.state === "on"),
  17  |     states.filter((state) => state.background !== "rgba(0, 0, 0, 0)"),
  18  |   ]) {
  19  |     expect(active.map((state) => state.value)).toEqual([selected]);
  20  |   }
  21  | }
  22  | 
  23  | test("task filters respond after navigating between workspace pages", async ({
  24  |   page,
  25  | }) => {
  26  |   const fixture = await createWorkspaceTaskFilterFixture(page, {
  27  |     includePending: true,
  28  |   });
  29  |   try {
  30  |     await gotoAndWaitForReady(page, "/workspace/homeworks");
  31  |     for (const tab of ["todos", "exams", "homeworks", "todos"] as const) {
  32  |       await page
  33  |         .locator(`a[href="/workspace/${tab}"]`)
  34  |         .filter({ visible: true })
  35  |         .first()
  36  |         .click();
  37  |       await page.waitForURL(`**/workspace/${tab}`);
  38  |       const group = page
  39  |         .locator('[data-slot="toggle-group"]')
  40  |         .filter({ has: page.locator('[data-value="incomplete"]') });
  41  |       for (const value of ["all", "completed", "incomplete"]) {
  42  |         await group.locator(`[data-value="${value}"]`).click();
  43  |         await page.mouse.move(0, 0);
  44  |         await expectSelection(group, value);
  45  |         await expect(
  46  |           page
  47  |             .getByText(fixture.completedTitle[tab], { exact: tab !== "exams" })
  48  |             .filter({ visible: true }),
  49  |         ).toHaveCount(value === "incomplete" ? 0 : 1);
  50  |       }
  51  |     }
  52  |   } finally {
  53  |     await fixture.cleanup();
  54  |   }
  55  | });
  56  | 
  57  | for (const tab of ["homeworks", "todos", "exams"] as const) {
  58  |   for (const mobile of [false, true]) {
  59  |     for (const includePending of [false, true]) {
  60  |       test(`${tab} filters actual ${includePending ? "mixed" : "completed-only"} data on ${mobile ? "mobile" : "desktop"}`, async ({
  61  |         page,
  62  |       }) => {
  63  |         await page.setViewportSize(
  64  |           mobile ? { width: 390, height: 844 } : { width: 1280, height: 900 },
  65  |         );
  66  |         const fixture = await createWorkspaceTaskFilterFixture(page, {
  67  |           includePending,
  68  |         });
  69  |         const errors: string[] = [];
  70  |         page.on("pageerror", (error) => errors.push(error.message));
  71  |         try {
  72  |           await gotoAndWaitForReady(page, `/workspace/${tab}`, {
  73  |             browserHealth: {},
  74  |           });
  75  |           const group = page
  76  |             .locator('[data-slot="toggle-group"]')
  77  |             .filter({ has: page.locator('[data-value="incomplete"]') });
  78  |           const completed = page
  79  |             .getByText(fixture.completedTitle[tab], { exact: tab !== "exams" })
  80  |             .filter({ visible: true });
  81  |           const pending = page
  82  |             .getByText(fixture.pendingTitle[tab], { exact: tab !== "exams" })
  83  |             .filter({ visible: true });
  84  |           await expectSelection(group, "incomplete");
  85  |           await expect(completed).toHaveCount(0);
  86  |           await expect(pending).toHaveCount(includePending ? 1 : 0);
  87  | 
  88  |           if (!includePending) {
  89  |             if (!mobile) {
  90  |               await expect(page.getByRole("table")).toBeVisible();
  91  |               await expect(
  92  |                 page.getByRole("columnheader").first(),
  93  |               ).toBeVisible();
  94  |             }
  95  |             await page
  96  |               .getByRole("button", { name: /清除筛选|Clear filter/i })
  97  |               .filter({ visible: true })
  98  |               .click();
  99  |             await expectSelection(group, "all");
> 100 |             await expect(completed).toHaveCount(1);
      |                                     ^ Error: expect(locator).toHaveCount(expected) failed
  101 |             await group.locator('[data-value="incomplete"]').click();
  102 |           }
  103 | 
  104 |           // Filtering loaded data must keep working without network access.
  105 |           await page.context().setOffline(true);
  106 |           for (const selected of ["all", "completed", "incomplete", "all"]) {
  107 |             const option = group.locator(`[data-value="${selected}"]`);
  108 |             for (const action of ["click", "click", "space"] as const) {
  109 |               if (action === "space") await option.press("Space");
  110 |               else await option.click();
  111 |               await page.mouse.move(0, 0);
  112 |               await expectSelection(group, selected);
  113 |               await expect(completed).toHaveCount(
  114 |                 selected === "incomplete" ? 0 : 1,
  115 |               );
  116 |               await expect(pending).toHaveCount(
  117 |                 includePending && selected !== "completed" ? 1 : 0,
  118 |               );
  119 |             }
  120 |           }
  121 |           await page.context().setOffline(false);
  122 |           // A fresh page uses the default filter, even if its result is empty.
  123 |           await gotoAndWaitForReady(page, `/workspace/${tab}`);
  124 |           await expectSelection(group, "incomplete");
  125 |           await expect(completed).toHaveCount(0);
  126 |           await expect(pending).toHaveCount(includePending ? 1 : 0);
  127 |           if (includePending && tab !== "exams") {
  128 |             const saved = page.waitForResponse(
  129 |               (response) =>
  130 |                 response.url().includes(`/${tab}/`) &&
  131 |                 ["POST", "PATCH", "PUT"].includes(response.request().method()),
  132 |             );
  133 |             await page
  134 |               .getByRole("button", { name: /标记为完成|Mark as complete/i })
  135 |               .filter({ visible: true })
  136 |               .click();
  137 |             expect((await saved).ok()).toBe(true);
  138 |             await page.mouse.move(0, 0);
  139 |             await expectSelection(group, "incomplete");
  140 |             await expect(pending).toHaveCount(0);
  141 |             await expect(completed).toHaveCount(0);
  142 |             await page
  143 |               .getByRole("button", { name: /清除筛选|Clear filter/i })
  144 |               .filter({ visible: true })
  145 |               .click();
  146 |             await expectSelection(group, "all");
  147 |             await expect(pending).toHaveCount(1);
  148 |             await expect(completed).toHaveCount(1);
  149 |             await gotoAndWaitForReady(page, `/workspace/${tab}`);
  150 |             await expectSelection(group, "incomplete");
  151 |             await expect(pending).toHaveCount(0);
  152 |             await group.locator('[data-value="completed"]').click();
  153 |             await expect(pending).toHaveCount(1);
  154 |           }
  155 |           expect(errors).toEqual([]);
  156 |         } finally {
  157 |           await page.context().setOffline(false);
  158 |           await fixture.cleanup();
  159 |         }
  160 |       });
  161 |     }
  162 |   }
  163 | }
  164 | 
```